Prior to Reopening into a container. 

You can modify your .bash_profile or .bash_aliases which will be loaded when the container spins up. You can also load any .ssh keys here if you want to use GitHub via SSH to clone repos.
